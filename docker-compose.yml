version: '3'

services:
  weather_forecast_retrieval:
    image: weather_forecast_retrieval
    build: .
    user: "${CURRENT_UID}:1"
    volumes:
      - /data:/data
      - /home/code/weather_forecast_retrieval/scripts:/code/config
